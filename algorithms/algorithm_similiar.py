# SELECT id
# FROM products as t1
# WHERE t1.category = %s
# AND t1.sub_category = %s
# AND t1.recommendable = true
# ORDER BY t1.aanbiedingen IS NOT NULL DESC
# LIMIT %s;
#
#
# SELECT id
# FROM products as t1
# WHERE t1.category = %s
# AND t1.recommendable = true
# ORDER BY t1.aanbiedingen IS NOT NULL DESC
# LIMIT %s;
